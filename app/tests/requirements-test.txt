# ─────────────────────────────────────────────────────────────────────────────
# requirements-test.txt
# Install with: pip install -r requirements-test.txt
# ─────────────────────────────────────────────────────────────────────────────

# Core test runner
pytest==8.1.1
pytest-django==4.8.0
pytest-mock==3.14.0
pytest-cov==5.0.0
pytest-xdist==3.5.0          # parallel test execution
pytest-timeout==2.3.1

# DRF + auth
djangorestframework==3.15.1
djangorestframework-simplejwt==5.3.1

# Celery testing
celery[redis]==5.3.6
# pytest-celery REMOVED — incompatible with Python 3.13 (uses deleted pkg_resources)
# Celery fixtures are defined manually in conftest.py instead

# Image generation (for real test images — not mocks)
Pillow>=12.0.0               # 10.x has no Python 3.13 wheels

# Cloudinary SDK
cloudinary==1.40.0

# HTTP client for integration / E2E tests
requests==2.31.0
httpx==0.27.0                 # async-friendly alternative

# Factory / fixtures
factory-boy==3.3.0
faker==24.11.0

# Coverage
coverage[toml]==7.4.4
